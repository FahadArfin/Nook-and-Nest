import {it,expect} from 'vitest';
import {NullEngine,Scene,MeshBuilder,TransformNode,StandardMaterial,Vector3} from '@babylonjs/core';
import {StaticPartInstances} from '../src/scene/StaticPartInstances';
it('shares static draw sources while preserving independent placements and materials',()=>{const engine=new NullEngine(),scene=new Scene(engine),pool=new StaticPartInstances();try{const a=new TransformNode('a',scene),b=new TransformNode('b',scene),box=MeshBuilder.CreateBox('box',{},scene);box.material=new StandardMaterial('white',scene);box.parent=a;box.position.x=2;const copy=box.clone('copy',b)!;pool.convert(a);pool.convert(b);const first=a.getChildMeshes()[0] as any,second=b.getChildMeshes()[0] as any;expect(first.sourceMesh).toBe(second.sourceMesh);first.position.x=4;expect(second.position.x).toBe(2);a.dispose();expect(second.isDisposed()).toBe(false);expect(second.sourceMesh.isDisposed()).toBe(false);expect(second.sourceMesh.isVisible).toBe(false);b.dispose();}finally{pool.dispose();scene.dispose();engine.dispose()}});
